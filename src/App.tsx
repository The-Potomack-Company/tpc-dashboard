export default function App() {
  return (
    <main className="flex items-center justify-center h-dvh bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100">
      <div className="text-center">
        <h1 className="text-2xl font-semibold">TPC Dashboard</h1>
        <p className="text-sm text-gray-500 dark:text-gray-400 mt-2">
          Scaffold ready. Auth and routes land in the next plan.
        </p>
      </div>
    </main>
  );
}
