// src/components/activity/UiRecentEventsFeed.tsx
// Phase 3 / D-32 dev — live tail of ui_interactions.
//
// STUB — Task 3 of 03-07-PLAN.md replaces this with the full live-tail feed
// (mirrors Phase 2 LiveEventFeed). This stub exists so the parent
// <UiInteractionsPanel> (Task 2) can import the module while tsc is type-checking.

export function UiRecentEventsFeed() {
  return null;
}
