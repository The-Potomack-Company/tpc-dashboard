// src/components/activity/UiInteractionsPanel.tsx
// Phase 3 / D-31 / D-32 — composition wrapper for the 4 ui_interactions sub-panels.
//
// STUB — Task 2 of 03-07-PLAN.md replaces this with the full composition wrapper
// (UiTopPagesTable + UiTopElementsTable + WalkthroughFunnel + UiRecentEventsFeed).
// This stub exists so the parent <DeveloperPanel> (Task 1) can import the module
// while tsc is type-checking. The DeveloperPanel tests mock this module, so the
// stub return value is irrelevant during testing.

export function UiInteractionsPanel() {
  return null;
}
