---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - index.html
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - eslint.config.js
  - .gitignore
  - .env.example
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - src/tests/setup.ts
  - src/components/.gitkeep
  - src/pages/.gitkeep
  - src/layouts/.gitkeep
  - src/lib/.gitkeep
  - src/db/.gitkeep
  - src/hooks/.gitkeep
  - src/stores/.gitkeep
  - src/services/.gitkeep
  - src/utils/.gitkeep
autonomous: true
requirements:
  - INFR-02
must_haves:
  truths:
    - "npm install completes with zero errors and pinned versions match TPC App"
    - "npm run build succeeds (tsc -b && vite build) and produces dist/"
    - "npm run lint exits 0 with zero warnings"
    - "npm test exits 0 (zero tests is acceptable; framework must be wired)"
    - "npm run dev serves the placeholder App.tsx at http://localhost:5173 without console errors"
  artifacts:
    - path: "package.json"
      provides: "Pinned runtime + dev deps, scripts dev/build/lint/test/preview/db:push/db:types"
      contains: "\"react\": \"^19.2.0\""
    - path: "vite.config.ts"
      provides: "React plugin + Tailwind v4 plugin + Vitest config block"
      contains: "@tailwindcss/vite"
    - path: "src/index.css"
      provides: "Tailwind import + @theme block with --color-accent"
      contains: "--color-accent: #2563eb"
    - path: "src/tests/setup.ts"
      provides: "@testing-library/jest-dom augmentation"
      contains: "@testing-library/jest-dom"
    - path: ".env.example"
      provides: "Env var template, checked into git"
      contains: "VITE_SUPABASE_URL"
  key_links:
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "src/index.css"
      to: "Tailwind v4 engine"
      via: "@import directive"
      pattern: "@import \"tailwindcss\""
---

<objective>
Scaffold the standalone Vite + React 19 + TypeScript + Tailwind v4 app with the exact version pins, directory skeleton, TypeScript configs, ESLint flat config, and Vitest test infrastructure matching TPC App. After this plan, `npm install`, `npm run build`, `npm run lint`, `npm test`, and `npm run dev` all succeed against an empty-but-valid app (a single placeholder `App.tsx`). No Supabase code, no routes, no auth yet — those arrive in later plans which depend on this scaffold.

Purpose: Establish the foundation (INFR-02 prerequisite) every subsequent plan in this phase builds on.
Output: A complete, installable, build-clean, lint-clean, test-ready project with pinned versions aligned to TPC App.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Reference file contents from TPC App sibling project. Executor should READ these files before writing the dashboard equivalents. They are the source of truth for every pattern choice in Phase 1. -->

TPC App reference paths (read-only reference, do NOT modify):
- c:/Users/maser/Projects/TPC_App/TPC_App/package.json          → version pins + scripts
- c:/Users/maser/Projects/TPC_App/TPC_App/vite.config.ts         → plugins + vitest test block
- c:/Users/maser/Projects/TPC_App/TPC_App/tsconfig.json          → composite references
- c:/Users/maser/Projects/TPC_App/TPC_App/tsconfig.app.json      → app compile settings
- c:/Users/maser/Projects/TPC_App/TPC_App/tsconfig.node.json     → vite.config compile settings
- c:/Users/maser/Projects/TPC_App/TPC_App/eslint.config.js       → flat config v9 shape
- c:/Users/maser/Projects/TPC_App/TPC_App/.gitignore             → exclusion patterns
- c:/Users/maser/Projects/TPC_App/TPC_App/src/index.css          → Tailwind v4 @theme block shape
- c:/Users/maser/Projects/TPC_App/TPC_App/src/tests/setup.ts     → @testing-library/jest-dom import
- c:/Users/maser/Projects/TPC_App/TPC_App/src/main.tsx           → root composition (reference; dashboard diverges in Plan 02)

Pinned versions (from RESEARCH.md Standard Stack, exact):
  react ^19.2.0 · react-dom ^19.2.0
  react-router ^7.13.1
  @supabase/supabase-js ^2.101.1
  @tanstack/react-query ^5.99.2 · @tanstack/react-query-devtools ^5.99.2
  zustand ^5.0.11 · zod ^4.3.6

Dev:
  typescript ~5.9.3 · vite ^7.3.1 · @vitejs/plugin-react ^5.1.1
  tailwindcss ^4.2.1 · @tailwindcss/vite ^4.2.1
  vitest ^4.0.18 · @testing-library/react ^16.3.2 · @testing-library/user-event ^14.6.1
  @testing-library/jest-dom ^6.9.1 · jsdom ^28.1.0
  eslint ^9.39.1 · @eslint/js ^9.39.1 · eslint-plugin-react-hooks ^7.0.1
  eslint-plugin-react-refresh ^0.4.24 · typescript-eslint ^8.48.0 · globals ^16.5.0
  supabase ^2.81.3
  @types/react ^19.2.7 · @types/react-dom ^19.2.3 · @types/node ^24.10.1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TS baseline and install all pinned dependencies</name>
  <files>
    package.json,
    package-lock.json,
    index.html,
    tsconfig.json,
    tsconfig.app.json,
    tsconfig.node.json,
    .gitignore,
    .env.example,
    public/vite.svg
  </files>
  <read_first>
    - c:/Users/maser/Projects/tpc-dashboard/CLAUDE.md (pinned versions are authoritative)
    - c:/Users/maser/Projects/tpc-dashboard/.planning/phases/01-foundation-auth/01-RESEARCH.md#Standard-Stack (exact version pins + install commands)
    - c:/Users/maser/Projects/TPC_App/TPC_App/package.json (mirror scripts + dev deps; this is the source of truth for TPC App alignment)
    - c:/Users/maser/Projects/TPC_App/TPC_App/tsconfig.json
    - c:/Users/maser/Projects/TPC_App/TPC_App/tsconfig.app.json
    - c:/Users/maser/Projects/TPC_App/TPC_App/tsconfig.node.json
    - c:/Users/maser/Projects/TPC_App/TPC_App/.gitignore
  </read_first>
  <action>
    1. From `c:/Users/maser/Projects/tpc-dashboard/`, run:
       `npm create vite@7.3.1 . -- --template react-ts`
       When prompted about the non-empty directory (because `.planning/`, `CLAUDE.md`, and `.git/` already exist), choose the option that keeps existing files ("Ignore files and continue"). Vite will write `package.json`, `index.html`, `tsconfig*.json`, `vite.config.ts`, `src/App.tsx`, `src/main.tsx`, `src/index.css`, `src/App.css` (delete later), `src/assets/react.svg` (delete later), `public/vite.svg`.
    2. Overwrite the generated `package.json` so "name": "tpc-dashboard", "private": true, "type": "module", "version": "0.0.0" and the scripts block exactly equals:
       ```json
       "scripts": {
         "dev": "vite",
         "build": "tsc -b && vite build",
         "lint": "eslint .",
         "test": "vitest --run",
         "preview": "vite preview",
         "db:push": "supabase db push",
         "db:types": "supabase gen types --lang=typescript --schema public > src/db/database.types.ts"
       }
       ```
    3. Pin runtime deps in `package.json` `dependencies`:
       ```json
       "react": "^19.2.0",
       "react-dom": "^19.2.0",
       "react-router": "^7.13.1",
       "@supabase/supabase-js": "^2.101.1",
       "@tanstack/react-query": "^5.99.2",
       "zustand": "^5.0.11",
       "zod": "^4.3.6"
       ```
    4. Pin `devDependencies` (full list from RESEARCH.md Standard Stack — do NOT omit any):
       ```json
       "@eslint/js": "^9.39.1",
       "@tailwindcss/vite": "^4.2.1",
       "@tanstack/react-query-devtools": "^5.99.2",
       "@testing-library/jest-dom": "^6.9.1",
       "@testing-library/react": "^16.3.2",
       "@testing-library/user-event": "^14.6.1",
       "@types/node": "^24.10.1",
       "@types/react": "^19.2.7",
       "@types/react-dom": "^19.2.3",
       "@vitejs/plugin-react": "^5.1.1",
       "eslint": "^9.39.1",
       "eslint-plugin-react-hooks": "^7.0.1",
       "eslint-plugin-react-refresh": "^0.4.24",
       "globals": "^16.5.0",
       "jsdom": "^28.1.0",
       "supabase": "^2.81.3",
       "tailwindcss": "^4.2.1",
       "typescript": "~5.9.3",
       "typescript-eslint": "^8.48.0",
       "vite": "^7.3.1",
       "vitest": "^4.0.18"
       ```
    5. Delete `src/App.css` and `src/assets/react.svg` (Vite-template leftovers we will not use).
    6. Update `index.html` `<title>` to `TPC Dashboard` and keep the single `<div id="root"></div>` + `<script type="module" src="/src/main.tsx"></script>` elements.
    7. Replace `tsconfig.json` to be a composite project referencing app + node:
       ```json
       {
         "files": [],
         "references": [
           { "path": "./tsconfig.app.json" },
           { "path": "./tsconfig.node.json" }
         ]
       }
       ```
    8. Write `tsconfig.app.json` mirroring TPC App's (strict, ES2022, Bundler module resolution, React jsx, include `src`, exclude `src/tests`). Copy TPC App's file contents verbatim.
    9. Write `tsconfig.node.json` mirroring TPC App's (strict, includes `vite.config.ts`). Copy TPC App's file contents verbatim.
    10. Write `.gitignore` by copying TPC App's `.gitignore` verbatim (already ensures `node_modules`, `dist`, `.env`, `.env.local`, coverage, logs are ignored).
    11. Write `.env.example`:
        ```
        VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
        VITE_SUPABASE_ANON_KEY=your-anon-key-here
        ```
    12. Run `npm install` to generate `package-lock.json` and populate `node_modules/` with the exact pinned versions.
    13. Verify: `cat node_modules/react/package.json | grep '"version"'` shows a `19.2.x` value; `cat node_modules/vite/package.json | grep '"version"'` shows a `7.3.x` value; `cat node_modules/@tailwindcss/vite/package.json | grep '"version"'` shows a `4.2.x` value.
    14. Do NOT run `npx supabase init` here — Plan 03 handles Supabase bootstrap.
  </action>
  <verify>
    <automated>npm install --no-audit --no-fund &amp;&amp; test -f package-lock.json &amp;&amp; node -e "const p=require('./package.json'); const req=['dev','build','lint','test','preview','db:push','db:types']; for(const s of req){ if(!p.scripts[s]) throw new Error('missing script '+s);} const runtime=['react','react-dom','react-router','@supabase/supabase-js','@tanstack/react-query','zustand','zod']; for(const d of runtime){ if(!p.dependencies[d]) throw new Error('missing dep '+d);} const dev=['@eslint/js','@tailwindcss/vite','@tanstack/react-query-devtools','@testing-library/jest-dom','@testing-library/react','@testing-library/user-event','@types/node','@types/react','@types/react-dom','@vitejs/plugin-react','eslint','eslint-plugin-react-hooks','eslint-plugin-react-refresh','globals','jsdom','supabase','tailwindcss','typescript','typescript-eslint','vite','vitest']; for(const d of dev){ if(!p.devDependencies[d]) throw new Error('missing devDep '+d);} console.log('deps OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists and contains `"name": "tpc-dashboard"` (grep: `grep -q '"name": "tpc-dashboard"' package.json`)
    - `package.json` pins `"react": "^19.2.0"` exactly (grep: `grep -q '"react": "\^19.2.0"' package.json`)
    - `package.json` pins `"vite": "^7.3.1"` exactly
    - `package.json` pins `"@tailwindcss/vite": "^4.2.1"`
    - `package.json` pins `"@tanstack/react-query": "^5.99.2"`
    - `package.json` pins `"react-router": "^7.13.1"` (NOT `react-router-dom`)
    - `package.json` scripts contains exactly: `dev`, `build`, `lint`, `test`, `preview`, `db:push`, `db:types`
    - `package.json` `build` script equals `"tsc -b && vite build"`
    - `package.json` `test` script equals `"vitest --run"` (no watch flag)
    - `tsconfig.json` has `"references"` referencing `./tsconfig.app.json` AND `./tsconfig.node.json`
    - `tsconfig.app.json` has `"exclude": ["src/tests"]`
    - `.gitignore` includes `node_modules`, `dist`, `.env`, `.env.local`
    - `.env.example` includes `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`
    - `index.html` `<title>` is `TPC Dashboard`
    - `package-lock.json` exists after `npm install`
    - `node_modules/react/package.json` version starts with `19.2.`
    - `node_modules/vite/package.json` version starts with `7.3.`
    - `node_modules/@tailwindcss/vite/package.json` version starts with `4.2.`
    - `src/App.css` does NOT exist (deleted)
    - `src/assets/react.svg` does NOT exist (deleted)
  </acceptance_criteria>
  <done>Project root contains pinned `package.json`, matching tsconfigs, `.gitignore`, `.env.example`, and `node_modules` populated. `npm install` runs clean. No Tailwind, no ESLint config, no src/ code yet — those are Tasks 2 and 3.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Tailwind v4, Vitest, ESLint flat config, and directory skeleton</name>
  <files>
    vite.config.ts,
    eslint.config.js,
    src/index.css,
    src/vite-env.d.ts,
    src/tests/setup.ts,
    src/components/.gitkeep,
    src/pages/.gitkeep,
    src/layouts/.gitkeep,
    src/lib/.gitkeep,
    src/db/.gitkeep,
    src/hooks/.gitkeep,
    src/stores/.gitkeep,
    src/services/.gitkeep,
    src/utils/.gitkeep
  </files>
  <read_first>
    - c:/Users/maser/Projects/TPC_App/TPC_App/vite.config.ts (plugins array + test block — mirror exactly, adjust only setupFiles path)
    - c:/Users/maser/Projects/TPC_App/TPC_App/eslint.config.js (flat v9 config; copy verbatim then adjust project references if needed)
    - c:/Users/maser/Projects/TPC_App/TPC_App/src/index.css (reference the @theme block shape; dashboard uses only --color-accent and --color-accent-hover per UI-SPEC)
    - c:/Users/maser/Projects/TPC_App/TPC_App/src/tests/setup.ts (bare-minimum @testing-library/jest-dom import)
    - c:/Users/maser/Projects/tpc-dashboard/.planning/phases/01-foundation-auth/01-UI-SPEC.md#Design-System (confirm --color-accent = #2563eb and --color-accent-hover = #1d4ed8)
    - c:/Users/maser/Projects/tpc-dashboard/.planning/phases/01-foundation-auth/01-RESEARCH.md#Pattern-5-Tailwind-v4-via-@tailwindcss/vite
    - c:/Users/maser/Projects/tpc-dashboard/vite.config.ts (current state from Vite scaffold — will be replaced)
  </read_first>
  <action>
    1. Replace `vite.config.ts` with (verbatim):
       ```typescript
       /// &lt;reference types="vitest/config" /&gt;
       import { defineConfig } from 'vite';
       import react from '@vitejs/plugin-react';
       import tailwindcss from '@tailwindcss/vite';

       export default defineConfig({
         plugins: [react(), tailwindcss()],
         test: {
           globals: true,
           environment: 'jsdom',
           setupFiles: ['src/tests/setup.ts'],
         },
       });
       ```
    2. Replace `src/index.css` (Vite scaffold wrote a default) with:
       ```css
       @import "tailwindcss";

       @theme {
         --color-accent: #2563eb;
         --color-accent-hover: #1d4ed8;
       }
       ```
       Do NOT add any additional custom tokens; UI-SPEC allows ONLY these two.
    3. Ensure `src/vite-env.d.ts` exists (Vite scaffold writes it). Leave contents as the default `/// <reference types="vite/client" />`.
    4. Create `src/tests/setup.ts`:
       ```typescript
       import '@testing-library/jest-dom/vitest';
       ```
    5. Create empty placeholder files to lock the directory skeleton from CONTEXT.md into git:
       - `src/components/.gitkeep`
       - `src/pages/.gitkeep`
       - `src/layouts/.gitkeep`
       - `src/lib/.gitkeep`
       - `src/db/.gitkeep`
       - `src/hooks/.gitkeep`
       - `src/stores/.gitkeep`
       - `src/services/.gitkeep`
       - `src/utils/.gitkeep`
       (Each `.gitkeep` has zero bytes.)
    6. Replace `eslint.config.js` with the flat-config contents from TPC App's `eslint.config.js`. Copy verbatim. The TPC App config already targets `.ts`/`.tsx` files with `typescript-eslint`, `react-hooks`, `react-refresh`, and `globals.browser` — no edits needed.
    7. Do NOT create `tailwind.config.js`. Tailwind v4 uses `@theme` in CSS; there is no JS config file (per RESEARCH.md Pitfall: no `tailwind.config.js`).
    8. Do NOT add `postcss.config.js`. Tailwind v4 via `@tailwindcss/vite` bundles its own pipeline (per RESEARCH.md Pattern 5).
  </action>
  <verify>
    <automated>test -f vite.config.ts &amp;&amp; test -f eslint.config.js &amp;&amp; test -f src/index.css &amp;&amp; test -f src/tests/setup.ts &amp;&amp; grep -q "@tailwindcss/vite" vite.config.ts &amp;&amp; grep -q "jsdom" vite.config.ts &amp;&amp; grep -q "src/tests/setup.ts" vite.config.ts &amp;&amp; grep -q "@import \"tailwindcss\"" src/index.css &amp;&amp; grep -q "#2563eb" src/index.css &amp;&amp; grep -q "@testing-library/jest-dom" src/tests/setup.ts &amp;&amp; test -f src/components/.gitkeep &amp;&amp; test -f src/pages/.gitkeep &amp;&amp; test -f src/layouts/.gitkeep &amp;&amp; test -f src/lib/.gitkeep &amp;&amp; test -f src/db/.gitkeep &amp;&amp; test -f src/hooks/.gitkeep &amp;&amp; test -f src/stores/.gitkeep &amp;&amp; test -f src/services/.gitkeep &amp;&amp; test -f src/utils/.gitkeep &amp;&amp; test ! -f tailwind.config.js &amp;&amp; test ! -f postcss.config.js &amp;&amp; echo "scaffold-files OK"</automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.ts` imports `@tailwindcss/vite` and registers it in `plugins: [react(), tailwindcss()]`
    - `vite.config.ts` has a `test:` block with `environment: 'jsdom'` and `setupFiles: ['src/tests/setup.ts']`
    - `vite.config.ts` starts with `/// <reference types="vitest/config" />`
    - `src/index.css` contains `@import "tailwindcss";` on a line
    - `src/index.css` contains `--color-accent: #2563eb;`
    - `src/index.css` contains `--color-accent-hover: #1d4ed8;`
    - `src/index.css` does NOT contain any other `--color-*` tokens (strict UI-SPEC compliance)
    - `src/tests/setup.ts` imports `@testing-library/jest-dom/vitest`
    - All nine `.gitkeep` files exist under `src/{components,pages,layouts,lib,db,hooks,stores,services,utils}/`
    - `tailwind.config.js` does NOT exist (v4 pattern)
    - `postcss.config.js` does NOT exist (v4 pattern)
    - `eslint.config.js` exists and exports a flat-config array (contains `export default`)
  </acceptance_criteria>
  <done>Tailwind v4 is wired via Vite plugin, Vitest config is ready, ESLint flat config is in place, test setup is wired, and the `src/` directory skeleton matches TPC App exactly. No code logic in any src file yet beyond `main.tsx` / `App.tsx` placeholders (handled in Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Write placeholder main.tsx + App.tsx, validate full build/lint/test chain</name>
  <files>
    src/main.tsx,
    src/App.tsx
  </files>
  <read_first>
    - c:/Users/maser/Projects/TPC_App/TPC_App/src/main.tsx (reference composition; dashboard version will EVOLVE in Plan 02 to add BrowserRouter + QueryClientProvider + auth init — do NOT copy that here)
    - c:/Users/maser/Projects/TPC_App/TPC_App/src/App.tsx (reference; dashboard App.tsx in Plan 02/04 adds routes)
    - c:/Users/maser/Projects/tpc-dashboard/src/main.tsx (current state from Vite scaffold — will be replaced)
    - c:/Users/maser/Projects/tpc-dashboard/src/App.tsx (current state from Vite scaffold — will be replaced)
    - c:/Users/maser/Projects/tpc-dashboard/package.json (confirm scripts work)
  </read_first>
  <action>
    1. Replace `src/main.tsx` with a minimal React 19 mount (NO router, NO QueryClient, NO auth — those arrive in Plan 02):
       ```typescript
       import { StrictMode } from 'react';
       import { createRoot } from 'react-dom/client';
       import './index.css';
       import App from './App';

       createRoot(document.getElementById('root')!).render(
         &lt;StrictMode&gt;
           &lt;App /&gt;
         &lt;/StrictMode&gt;,
       );
       ```
    2. Replace `src/App.tsx` with a minimal placeholder:
       ```typescript
       export default function App() {
         return (
           &lt;main className="flex items-center justify-center h-dvh bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100"&gt;
             &lt;div className="text-center"&gt;
               &lt;h1 className="text-2xl font-semibold"&gt;TPC Dashboard&lt;/h1&gt;
               &lt;p className="text-sm text-gray-500 dark:text-gray-400 mt-2"&gt;
                 Scaffold ready. Auth and routes land in the next plan.
               &lt;/p&gt;
             &lt;/div&gt;
           &lt;/main&gt;
         );
       }
       ```
    3. Run the full scaffold validation chain in order, stopping on first failure:
       - `npm run lint` → must exit 0 with zero errors. If `eslint.config.js` complains about missing project tsconfig, match TPC App's exact config shape.
       - `npm run build` → must exit 0. Produces `dist/`. If `tsc -b` fails on tsconfig references, confirm `tsconfig.app.json` / `tsconfig.node.json` match TPC App exactly.
       - `npm test` → must exit 0 (with message like "No test files found, exiting with code 0" OR a future-proof pattern: because Vitest 4 exits 0 when no tests and no `passWithNoTests` override is hostile; verify with `vitest --run` directly — if it fails due to "no test files", add `passWithNoTests: true` to the `test:` block in vite.config.ts).
    4. Do NOT start `npm run dev` in this automated task (it blocks). Manual smoke-test is part of the Plan 05 QA checklist.
    5. Add `dist/` to `.gitignore` if not already present (TPC App's .gitignore already has it; confirm).
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; test -d dist &amp;&amp; test -f dist/index.html &amp;&amp; npm test -- --passWithNoTests &amp;&amp; grep -q "TPC Dashboard" src/App.tsx &amp;&amp; grep -q "StrictMode" src/main.tsx &amp;&amp; grep -q "createRoot" src/main.tsx &amp;&amp; echo "scaffold validated"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.tsx` imports `createRoot` from `react-dom/client` and `StrictMode` from `react`
    - `src/main.tsx` imports `./index.css`
    - `src/main.tsx` does NOT import `BrowserRouter` yet (that's Plan 02)
    - `src/main.tsx` does NOT import `QueryClientProvider` yet (that's Plan 02)
    - `src/main.tsx` does NOT import `authStore` or `supabase` yet (those files don't exist)
    - `src/App.tsx` exports a default function component
    - `src/App.tsx` renders `&lt;h1&gt;TPC Dashboard&lt;/h1&gt;`
    - `npm run lint` exits 0 (zero errors; warnings acceptable but aim for zero)
    - `npm run build` exits 0 AND produces `dist/index.html` AND `dist/assets/` with hashed JS+CSS bundles
    - `npm test -- --passWithNoTests` exits 0 (no test files yet is fine)
    - `dist/` is listed in `.gitignore`
  </acceptance_criteria>
  <done>Scaffold is fully installable, lint-clean, build-clean, and test-runnable. A developer running `npm run dev` would see the placeholder at http://localhost:5173. Plan 02 will replace `main.tsx` and `App.tsx` with the real composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → package registry (npm) | Third-party packages crossing into the project; pinned versions mitigate supply-chain drift |
| Build output (dist/) → client browsers | Client bundle ships JS (including any env vars prefixed `VITE_`); anon keys are intentionally public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.local` containing `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` | mitigate | `.gitignore` contains `.env` and `.env.local` (copied from TPC App); `.env.example` is a template only, no real secrets. Service role key is NEVER referenced in Phase 1 frontend (verified by grepping src/). |
| T-01-02 | Tampering | Dependency drift (auto-upgrade pulls breaking version) | mitigate | All versions pinned with `^` or `~` per CLAUDE.md; `package-lock.json` committed (Task 1) locks exact transitive versions. |
| T-01-06 | Elevation of Privilege | Missing `.gitignore` entries leak credentials | mitigate | `.gitignore` copied verbatim from TPC App (known-good), extended with `dist/`. |
| T-01-rate | n/a | Supabase Auth rate limiting | transfer | Supabase Auth handles credential-stuffing protection server-side; no Phase 1 code needed. Recorded as external control. |
</threat_model>

<verification>
After Task 3 completes, run all three as a single wave check:

```bash
npm install --no-audit --no-fund
npm run lint
npm run build
npm test -- --passWithNoTests
```

All four must exit 0. If any fails, the scaffold is broken and subsequent plans cannot run — fix before committing.

Manual smoke (not automated in this plan; logged for Plan 05 QA checklist):
- `npm run dev` serves http://localhost:5173 showing the "Scaffold ready" placeholder with no console errors.
- The page has the expected background (white in light mode, dark gray in dark mode — Tailwind v4 utilities compile successfully).
</verification>

<success_criteria>
- `package.json` pins all versions exactly per RESEARCH.md Standard Stack (spot-check 5+ packages)
- Scripts `dev`, `build`, `lint`, `test`, `preview`, `db:push`, `db:types` present
- `vite.config.ts` wires Tailwind v4 plugin + Vitest jsdom + setup file
- `src/index.css` has ONLY `@import "tailwindcss"` + `@theme { --color-accent, --color-accent-hover }`
- `src/tests/setup.ts` imports `@testing-library/jest-dom/vitest`
- All nine `src/<dir>/.gitkeep` placeholder files exist
- `eslint.config.js` is a flat-config `export default` that passes `npm run lint` against the placeholder `App.tsx`/`main.tsx`
- `npm run build` produces `dist/index.html` and a hashed JS+CSS bundle
- No `tailwind.config.js`, no `postcss.config.js`, no `react-router-dom` (v6 package)
- `.env.example` tracked in git; `.env.local` ignored
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md` following the standard summary template. Include:
- Pinned versions table (actual resolved versions from `node_modules/*/package.json`)
- Any deviations from TPC App (there should be none; document if any)
- A note confirming Plan 02 is unblocked (main.tsx and App.tsx are placeholders awaiting Supabase+auth wiring)
</output>
