---
phase: 02-pdf-import-pipeline
plan: 05
type: execute
wave: 5
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - README.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - DATA-07
tags: [docs, manual-qa, release]

must_haves:
  truths:
    - "README documents: prerequisites (service role key), how to run import, dry-run vs live, where PDFs live, what gets logged"
    - "PROJECT.md PDF source path corrected from ~/Desktop/rfc_profiles to ~/Projects/rfc_profiles/rfc_profiles"
    - "STATE.md updated: Phase 2 complete, resume stopped at next phase"
    - "REQUIREMENTS.md traceability table marks DATA-01..07 as Complete (or Partial with explicit note)"
    - "Human has run the live full import against the 457 PDFs and captured scraper_runs summary"
    - "Human has spot-checked at least 10 sales: values in DB match values in source PDFs"
    - "Human has confirmed auto-discovered department codes rendered correctly (no code = null or empty display_name crashes)"
  artifacts:
    - path: "README.md"
      provides: "User-facing docs for the import pipeline"
      contains: "npm run import:pdfs"
      min_lines: 40
    - path: ".planning/PROJECT.md"
      provides: "Corrected PDF source path"
      contains: "rfc_profiles/rfc_profiles"
    - path: ".planning/STATE.md"
      provides: "Phase 2 closing state"
      contains: "Phase 2"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "instruction to copy to .env.local and fill SUPABASE_SERVICE_ROLE_KEY"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 2"
      via: "traceability table status column"
      pattern: "DATA-0[1-7].*(Complete|Partial)"
---

<objective>
Close out Phase 2: docs update, manual QA of the full 457-PDF run, and requirement status updates. Everything from Waves 1-4 is assembled — this wave proves it works end-to-end against real data and records the evidence.

Purpose: DATA-01 (bulk import), DATA-02 (sale parse), DATA-03 (dept parse), DATA-05 (cross-validate), DATA-06 (departments seeded + auto-discovered), DATA-07 (dedupe) are all at least partially tested; this wave provides the real-data validation + sign-off that moves them from Pending to Complete.

Output: Live DB containing 395+ real sale records (457 minus 62 empty placeholders), a scraper_runs row summarizing the run, a spot-check log confirming 10+ sales round-trip correctly, and updated planning docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-pdf-import-pipeline/02-CONTEXT.md
@.planning/phases/02-pdf-import-pipeline/02-RESEARCH.md
@.planning/phases/02-pdf-import-pipeline/02-VALIDATION.md
@README.md
@.env.example
@scripts/import-pdfs.ts

<interfaces>
<!-- Docs to update — existing content: -->

.planning/PROJECT.md line 48:
  ...RFC Auction Profiles: 457 PDFs in ~/Desktop/rfc_profiles/...
  ^^^^ incorrect path per CONTEXT.md decision. Must become:
  ...RFC Auction Profiles: 457 PDFs in ~/Projects/rfc_profiles/rfc_profiles/...

.planning/STATE.md stopped_at + last_updated fields.

.planning/REQUIREMENTS.md traceability table rows for DATA-01..07 currently say "Pending".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README, PROJECT.md, STATE.md</name>
  <read_first>
    - README.md (current contents — what already exists)
    - .planning/PROJECT.md (line 48 has the incorrect path ~/Desktop/rfc_profiles)
    - .planning/STATE.md (current Position + stopped_at fields)
    - scripts/import-pdfs.ts (flag list — for README documentation)
    - .env.example (env var names + comments — for README prerequisites)
  </read_first>
  <files>
    README.md,
    .planning/PROJECT.md,
    .planning/STATE.md
  </files>
  <action>
    **README.md** — Add a top-level "## Importing Auction Profile PDFs" section with:

    1. **Prerequisites:**
       - Copy `.env.example` to `.env.local`, paste Supabase service role key
       - PDF files at `$HOME/Projects/rfc_profiles/rfc_profiles/` (or pass `--source <dir>`)
       - Node 18+ (built-in parseArgs), npm

    2. **Commands:**
       ```
       # Dry-run (safe, no DB writes):
       npm run import:pdfs -- --dry-run --limit 10

       # Full import (writes to DB):
       npm run import:pdfs

       # Subset:
       npm run import:pdfs -- --limit 50

       # Verbose logging:
       npm run import:pdfs -- --verbose
       ```

    3. **What happens during import:**
       - Reads every `.PDF` under `--source`
       - Parses page 1 as the "All Departments" sale summary, pages 2..N as per-department breakdowns
       - Cross-validates dept sums against sale totals (tolerance ±$0.25); flags mismatches with `validation_warning=true`
       - Auto-discovers unknown department codes (inserts into `departments` with `auto_discovered=true`)
       - Skips duplicates (same `sale_number`) and empty placeholders (1182-byte files with no text)
       - Logs each invocation to `scraper_runs` table

    4. **Inspecting results:**
       ```sql
       -- How many sales got flagged for manual review?
       select count(*) from sales where validation_warning = true;

       -- Which department codes were auto-discovered?
       select code, display_name from departments where auto_discovered = true;

       -- Most recent import run:
       select status, started_at, finished_at, sales_imported, sales_found, logs
       from scraper_runs order by started_at desc limit 1;
       ```

    5. **Troubleshooting:**
       - Missing `SUPABASE_SERVICE_ROLE_KEY`: copy `.env.example` to `.env.local`, paste key from Supabase dashboard → Project Settings → API → service_role
       - Parse failures on specific files: run with `--verbose` to see the error per file; many are expected (empty placeholders classified as `skipped: empty`)
       - Re-running safely: yes, `--limit 10` on a fresh DB inserts 10; second run skips all 10 (DATA-07)

    6. **Security warning block:**
       - `SUPABASE_SERVICE_ROLE_KEY` bypasses RLS. Keep in `.env.local`, NEVER commit, NEVER prefix with `VITE_`.
       - Rotate the key in Supabase dashboard if it was ever exposed.

    **PROJECT.md** — Edit line 48 (the "RFC Auction Profiles" bullet in Context section). Change:
      `~/Desktop/rfc_profiles/`
    to:
      `~/Projects/rfc_profiles/rfc_profiles/`
    Also update the "Last updated" footer to today's date (2026-04-21).

    **STATE.md** — Update:
    - `stopped_at`: "Phase 2 complete; ready to plan Phase 3"
    - `last_updated`: today's ISO timestamp
    - `last_activity`: today
    - `progress.completed_phases`: 1 → 2 after Task 3 flips the roadmap
    - `Current Position.Phase`: 2 of 10 → 3 of 10 (sale views)
    - Under `Decisions`, add 3 new bullets:
      - [Phase 2]: PDF source path corrected to $HOME/Projects/rfc_profiles/rfc_profiles (PROJECT.md updated).
      - [Phase 2]: import_sale_with_departments RPC is security-definer with execute grant to service_role only — reuse for Phase 10 scraper.
      - [Phase 2]: Cross-validation tolerance ±$0.25 empirically held; no widening needed.
    - Under `Blockers/Concerns`: REMOVE `Phase 2 (PDF Import): PDF format variance across 457 files is unverified at scale` (now resolved).
  </action>
  <verify>
    <automated>grep -q "npm run import:pdfs" README.md &amp;&amp; grep -q "SUPABASE_SERVICE_ROLE_KEY" README.md &amp;&amp; grep -q "validation_warning" README.md &amp;&amp; grep -q "auto_discovered" README.md &amp;&amp; grep -q "rfc_profiles/rfc_profiles" .planning/PROJECT.md &amp;&amp; ! grep -q "Desktop/rfc_profiles" .planning/PROJECT.md &amp;&amp; grep -q "Phase 2" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a new "Importing Auction Profile PDFs" section with prerequisites, commands, results-inspection SQL, troubleshooting, security warning
    - README.md references SUPABASE_SERVICE_ROLE_KEY and .env.example
    - PROJECT.md line referencing PDF source path no longer mentions Desktop — only ~/Projects/rfc_profiles/rfc_profiles
    - PROJECT.md "Last updated" footer reflects today's date
    - STATE.md stopped_at mentions Phase 2 complete
    - STATE.md Decisions section has 3 new Phase 2 bullets
    - STATE.md Blockers/Concerns no longer mentions "PDF format variance across 457 files is unverified"
  </acceptance_criteria>
  <done>Docs up to date. Ready for human verification + the live import checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run full 457-PDF import and spot-check 10 sales</name>
  <read_first>
    - scripts/import-pdfs.ts (CLI entry point)
    - .env.example (confirm SUPABASE_SERVICE_ROLE_KEY setup)
    - .planning/phases/02-pdf-import-pipeline/02-RESEARCH.md Pitfall 2 (62 empty placeholders expected)
    - .planning/phases/02-pdf-import-pipeline/02-RESEARCH.md Pitfall 6 (±$0.25 tolerance)
  </read_first>
  <files>
    (no files modified by this task — produces DB state + pasted report in chat)
  </files>
  <action>
    Human-only step (requires real PDF files on local disk + live Supabase + visual spot-check against the source PDFs). Claude cannot execute.

    **Step 1 — Sanity dry-run:**
    ```
    npm run import:pdfs -- --dry-run --limit 10
    ```
    Expected: 10 progress lines, summary shows 10 dry_run_ok (with any empty placeholders in the first 10 alphabetical filenames counted as skipped:empty). Exit 0. No DB activity.

    **Step 2 — Small real run (canary):**
    ```
    npm run import:pdfs -- --limit 20
    ```
    Expected: confirmation banner prints, 3-second pause, 20 files processed. Most inserted; some skipped:empty possible (the first 20 files alphabetically include `1000_...`, `1086_...`, `1092_...`, `1111_...`, `10ES_...` which are all empty placeholders per RESEARCH.md). Exit 0.

    Verify in Supabase dashboard / psql:
    ```sql
    select sale_number, title, sale_date, lots_auctioned, net_revenue, validation_warning
    from sales order by imported_at desc limit 20;

    select sale_id, department_code, lots_sold, total_sold_value
    from sale_departments where sale_id in (select id from sales order by imported_at desc limit 5)
    order by sale_id, department_code;

    select code, display_name, auto_discovered from departments order by auto_discovered desc, code;

    select status, started_at, finished_at, sales_found, sales_imported, logs
    from scraper_runs order by started_at desc limit 1;
    ```

    **Step 3 — Full run:**
    ```
    npm run import:pdfs
    ```
    Expected: ~5-15 minutes runtime (pdf-parse is the bottleneck at ~1-2s per non-empty file). Final summary should show approximately: Inserted ~373-395, Skipped (duplicate) ~20 (from Step 2), Skipped (empty) ~62, Failed hopefully 0 but tolerate up to 5.

    **Step 4 — Spot-check 10 sales against source PDFs.** Pick 10 sales from the DB (mix of short and long, mix of IT* and numeric sale numbers):
    ```sql
    (select sale_number, id from sales where sale_number like 'IT%' order by random() limit 5)
    union all
    (select sale_number, id from sales where sale_number not like 'IT%' order by random() limit 5);
    ```
    For each, open the source PDF in a viewer and verify:
    - `sale_number` matches the file name prefix
    - `title`, `sale_date`, `lots_auctioned`, `lots_sold`, `total_sold_value`, `hammer_total`, `net_revenue` match the PDF
    - Department rows sum to sale-level totals (modulo the documented ±$0.25 drift)
    - If `validation_warning = true`, note which columns mismatched and whether the drift makes sense

    **Step 5 — Auto-discovered codes sanity:**
    ```sql
    select code, display_name from departments where auto_discovered = true;
    ```
    Expected: several of ANT, CNS, GAR, JWL, LIT, MANU, MIN, NAT, REL, RUG, TRI, FASH, ISL, ASNP per RESEARCH.md. Display_name should be the human-readable name from the PDF footer (e.g., "Garden Furniture & Accessories" for GAR), not just the code.

    **Step 6 — Re-run idempotency:**
    ```
    npm run import:pdfs
    ```
    Expected: summary shows Inserted=0, Skipped (duplicate)=~395, Skipped (empty)=62, status='success' (DATA-07).

    Report back in chat with:
    - Final scraper_runs summary (counts from Step 3)
    - Spot-check result table: 10 sale numbers, pass/fail per field (paste as a markdown table)
    - Auto-discovered codes list (Step 5 query output)
    - Confirmation that Step 6 re-run produced 0 inserts
  </action>
  <what-built>
    - Migrations + RPC deployed (Plan 01)
    - Parser + schemas + supabase-admin (Plan 02)
    - Import pipeline with cross-validate + idempotency + auto-discover (Plan 03)
    - CLI with progress UX + scraper_runs lifecycle (Plan 04)
    - Docs updated (Task 1 above)

    No live 457-PDF run has happened yet. All tests are green. Dry-run works.
  </what-built>
  <how-to-verify>
    Follow the 6 steps listed in `<action>` above exactly. Paste the required reports back into the chat.
  </how-to-verify>
  <verify>
    <automated>N/A — live-run checkpoint; no automated proxy possible. Verification proceeds via Task 3 grep against REQUIREMENTS.md / ROADMAP.md after human paste of the scraper_runs summary.</automated>
  </verify>
  <acceptance_criteria>
    - Step 3 full-run scraper_runs row: status in ('success', 'partial'), sales_imported >= 370
    - Step 3 summary: inserted + skipped_duplicate + skipped_empty + failed === 457 (exact)
    - Step 4 spot-check: 10/10 sales round-trip correctly (allow up to 2 fields per sale to mismatch within tolerance; note them)
    - Step 5: at least 5 auto-discovered codes (RESEARCH.md found 12+ in samples)
    - Step 6 re-run: sales_imported === 0, skipped_duplicate === (previous sales_imported)
  </acceptance_criteria>
  <done>Human has pasted scraper_runs summary, 10-sale spot-check table, auto-discovered codes, and re-run confirmation into the chat. Phase 2 behaviors all verified end-to-end against real data.</done>
  <resume-signal>
    Paste the scraper_runs summary, the spot-check markdown table, and the auto-discovered codes list. Type "verified" to approve, or describe issues for gap-closure planning.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md traceability + ROADMAP.md status</name>
  <read_first>
    - .planning/REQUIREMENTS.md (current status column for DATA-01..07)
    - .planning/ROADMAP.md (Phase 2 Plans section, checkbox state)
    - scraper_runs summary + spot-check report from Task 2 resume-signal
  </read_first>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>
  <action>
    **REQUIREMENTS.md** — For each DATA-0N row in the Traceability table, flip status from "Pending" to "Complete" if Task 2 verification passed. If any requirement needs scope notes:

    - DATA-01 (bulk import): Complete. Scraper_runs row count + sales_imported confirm.
    - DATA-02 (sale-level parse): Complete per 10-sale spot-check.
    - DATA-03 (per-dept parse): Complete per 10-sale spot-check.
    - DATA-04 (numeric edge cases): Complete per numeric.test.ts (20+ cases) + real-PDF round-trip.
    - DATA-05 (cross-validation): Complete. N sales flagged with validation_warning; spot-check confirmed flags are legitimate drift.
    - DATA-06 (departments seeded + auto-discover): Complete. 22 seeded + N auto-discovered (list them).
    - DATA-07 (dedupe): Complete per Task 2 Step 6 re-run.

    If any requirement only Partial per Task 2 issues, mark "Partial (Phase 2)" with a 1-line reason and the specific sale numbers that need gap closure. The orchestrator can then run `/gsd-plan-phase --gaps` for the outliers.

    **ROADMAP.md** — Phase 2 section:
    - Flip top-level checkbox: `- [ ] Phase 2: PDF Import Pipeline` → `- [x] Phase 2: PDF Import Pipeline`
    - Flip each `- [ ] 02-0N-PLAN.md` to `- [x]` under the Plans: list
    - Update Progress table at bottom: `Phase 2` Plans Complete column from `0/?` to `5/5`; Status from "Not started" to "Complete"; Completed column with today's date
    - Update the top-level progress counter if it exists (e.g., "1 of 10 complete" → "2 of 10 complete")
  </action>
  <verify>
    <automated>grep -q "DATA-01.*Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -q "DATA-07.*Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -q "\[x\].*Phase 2:" .planning/ROADMAP.md &amp;&amp; grep -q "\[x\] 02-05-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - All 7 DATA-XX rows in REQUIREMENTS.md traceability are Complete (or Partial with explicit reason)
    - ROADMAP.md Phase 2 top checkbox is `[x]`
    - All 5 Phase 2 plan checkboxes in ROADMAP.md are `[x]`
    - ROADMAP.md Progress table Phase 2 status is "Complete" with today's date
  </acceptance_criteria>
  <done>Phase 2 formally complete. Roadmap reflects 2/10 phases done. State/docs/reqs aligned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI invocation → live Supabase | Human-gated (Task 2 checkpoint). T-05 banner applies. |
| Docs → git history | Public once pushed; no secrets (README/PROJECT/STATE never carry keys). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05 | Tampering (wrong-DB target during live run) | Task 2 live run | mitigate | Confirmation banner from Plan 04. Operator reviews source dir + target URL + file count before proceeding. 3-second pause in CLI. |
| T-06 | Information disclosure (secrets in committed docs) | README.md, .env.example | mitigate | .env.example uses `<paste-service-role-secret-here>` placeholder — no real key. README explicitly warns against committing .env.local. grep-verified absence of any JWT-like string in docs before commit. |
| T-09 | Repudiation (no record of who ran the import) | scraper_runs row | accept | scraper_runs captures started_at, finished_at, counts. User identity is not captured in v1 (single-admin deployment). Phase 10 scraper should augment if multi-user. |
</threat_model>

<verification>
- [ ] Docs grep-verifications pass (Task 1 acceptance)
- [ ] Human-verified: live full run, 10-sale spot-check, auto-discovered codes, re-run idempotency (Task 2 acceptance)
- [ ] REQUIREMENTS + ROADMAP reflect Phase 2 complete (Task 3 acceptance)
- [ ] No secrets ever committed (grep dist/ and docs/ for anything matching eyJ[A-Za-z0-9_-]+\.eyJ — the JWT signature prefix — and if found, ABORT and rotate the key)
</verification>

<success_criteria>
Phase 2 closed. 457 source PDFs have been processed end-to-end. ~373-395 sales exist in the `sales` table with correct financial data (verified by spot-check). Per-dept breakdowns cross-validate within ±$0.25 for the majority of sales; outliers flagged with `validation_warning=true` and confirmed legitimate. `departments` table contains the 22 seed codes plus auto-discovered codes from the real PDFs. Re-running the import produces zero new inserts. Planning docs (PROJECT, STATE, REQUIREMENTS, ROADMAP) all reflect Phase 2 completion. Ready to plan Phase 3 (Sale Views).
</success_criteria>

<output>
After completion, create .planning/phases/02-pdf-import-pipeline/02-05-SUMMARY.md. Record: final scraper_runs row contents (copy the JSONB logs), 10-sale spot-check markdown table, complete list of auto-discovered department codes with their discovered display_names, tolerance-outlier sale numbers and what drifted, total phase duration, any issues discovered that warrant a Phase 2 follow-up (list for `/gsd-plan-phase --gaps`).
</output>
