---
phase: 06-department-analysis-sale-comparison
plan: 06
type: execute
wave: 3
depends_on: [06-02, 06-03, 06-04]
files_modified:
  - src/layouts/DashboardLayout.tsx
  - src/tests/dashboard-layout.test.tsx
autonomous: false
requirements:
  - INTR-01
must_haves:
  truths:
    - "DashboardLayout sidebar 'Departments' entry renders as an active <NavLink to='/departments'> (not aria-disabled span)"
    - "Active NavLink uses the same active-state styling Phase 5 established for Trends (accent left-edge indicator + text per accent reservation #3)"
    - "'Departments · Coming soon' aside is REMOVED; entry reads 'Departments' only"
    - "Team / Reports / Custom Charts remain aria-disabled spans with their Coming soon asides intact"
    - "dashboard-layout.test.tsx asserts the flip end-to-end: Departments is a navigable link, other placeholders are still disabled"
  artifacts:
    - path: src/layouts/DashboardLayout.tsx
      provides: "Sidebar nav with Departments as an active NavLink"
      exports: ["DashboardLayout"]
  key_links:
    - from: src/layouts/DashboardLayout.tsx
      to: /departments
      via: "NavLink to='/departments'"
      pattern: "to=\"/departments\""
---

<objective>
Activate the `Departments` sidebar entry in `DashboardLayout` — flip the
placeholder aria-disabled span into an active `<NavLink to="/departments">`,
mirroring the transition Phase 5 Plan 05-07 made for Trends.

Purpose: Completes the user-visible surface for Phase 6. Every preceding
plan (06-01 through 06-05) shipped the data, the pages, and the UI. This
plan unlocks navigation so the user can actually reach `/departments` from
the sidebar.

This is the final wave for Phase 6 — runs after 06-02, 06-03, and 06-04
complete so the page is fully functional before we expose it via nav.

Output:
- Modifications to `src/layouts/DashboardLayout.tsx` (one-entry flip)
- Augmented `src/tests/dashboard-layout.test.tsx` (verify flip + regression guard for other placeholders)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-department-analysis-sale-comparison/06-CONTEXT.md
@.planning/phases/06-department-analysis-sale-comparison/06-UI-SPEC.md
@.planning/phases/06-department-analysis-sale-comparison/06-02-SUMMARY.md
@.planning/phases/06-department-analysis-sale-comparison/06-03-SUMMARY.md
@.planning/phases/06-department-analysis-sale-comparison/06-04-SUMMARY.md
@.planning/phases/05-trend-analysis/05-07-SUMMARY.md
@src/layouts/DashboardLayout.tsx
@src/tests/dashboard-layout.test.tsx

<interfaces>
<!-- Existing shape in DashboardLayout -->

DashboardLayout currently renders a list of nav items; items with an active route use `<NavLink>` and items without use `<span aria-disabled="true">` with a `· Coming soon` aside. The "Departments" entry is currently the aria-disabled variant. This plan flips it to the NavLink variant. Target patterns are already established for Sales (Phase 3) and Trends (Phase 5 Plan 05-07).

No new contracts are introduced.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip Departments sidebar entry to active NavLink + augment test</name>
  <files>
    src/layouts/DashboardLayout.tsx,
    src/tests/dashboard-layout.test.tsx
  </files>
  <read_first>
    - src/layouts/DashboardLayout.tsx (current rendering — locate the Departments span and surrounding NavLink patterns for Sales/Trends)
    - src/tests/dashboard-layout.test.tsx (current assertions — must NOT regress)
    - .planning/phases/05-trend-analysis/05-07-SUMMARY.md (precedent for this exact flip)
    - .planning/phases/06-department-analysis-sale-comparison/06-UI-SPEC.md § Copywriting → Page titles + navigation (sidebar link copy)
  </read_first>
  <action>
    Modify `src/layouts/DashboardLayout.tsx`:

    1. Locate the current rendering for the Departments entry (an `aria-disabled="true"` span; identified by `grep -n "Departments" src/layouts/DashboardLayout.tsx` — current lines indicate it's around the sidebar entry list near line 81 + render around line 139).

    2. Replace the disabled-span branch for the Departments item with the active `<NavLink>` variant used for Sales/Trends. Exact shape (mirror the Sales/Trends NavLink):

    ```tsx
    <NavLink
      to="/departments"
      className={({ isActive }) => [
        'flex items-center gap-3 px-4 py-2 rounded-md text-sm font-semibold',
        isActive
          ? 'text-accent border-l-2 border-accent -ml-[2px] pl-[14px]'  /* accent reservation #3 */
          : 'text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-800',
      ].join(' ')}
    >
      <IconBuildingLibrary aria-hidden="true" className="w-5 h-5" />
      <span>Departments</span>
    </NavLink>
    ```

    The exact classes should match what Sales and Trends already use — copy their NavLink className builder verbatim.

    3. Remove the `· Coming soon` aside for Departments (the existing disabled variant has this — the active NavLink variant does not).

    4. DO NOT touch Team, Reports, or Custom Charts entries — they remain aria-disabled spans with their Coming soon asides.

    5. The top-of-file comment block should be UPDATED if it enumerates active/inactive items (e.g. `// Sales becomes an active NavLink to /sales` — add Departments to that list per 05-07's precedent).

    Augment `src/tests/dashboard-layout.test.tsx` with 3 NEW tests (keep all existing):

    - T-new-1: `getByRole('link', { name: /Departments/i })` resolves; its `href` attribute equals `/departments`.
    - T-new-2: the Departments entry is NOT an aria-disabled element (`expect(link.getAttribute('aria-disabled')).toBeNull();` OR `expect(within(link).queryByText(/Coming soon/i)).toBeNull();`).
    - T-new-3: Team, Reports, and Custom Charts remain aria-disabled with `Coming soon` asides (regression guard — use `queryAllByText('Coming soon')` and assert length === 3).
  </action>
  <verify>
    <automated>npx vitest run src/tests/dashboard-layout.test.tsx --reporter=dot && grep -c "to=\"/departments\"" src/layouts/DashboardLayout.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All existing dashboard-layout tests pass.
    - 3 new tests pass.
    - `grep -c 'to="/departments"' src/layouts/DashboardLayout.tsx` returns ≥ 1.
    - Departments entry has NO `aria-disabled` attribute.
    - Team, Reports, Custom Charts remain disabled placeholders with Coming soon asides (exactly 3 Coming soon occurrences).
    - `npx tsc --noEmit` passes.
    - Active-state class reuses accent reservation #3 (border-l-2 border-accent) — mirrors Sales/Trends.
  </acceptance_criteria>
  <done>Departments sidebar entry is now a live NavLink; Phase 6 navigation surface complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke check — end-to-end Phase 6 navigation</name>
  <files>(none — manual verification only)</files>
  <what-built>
    All Phase 6 plans are complete: data layer (06-01), /departments page with table + charts (06-02, 06-03), /sales selection + /sales/compare (06-04), Revenue Breakdown on Sale Detail (06-05), and the sidebar flip (this plan). Task 1 has been automated and unit-tested.
  </what-built>
  <how-to-verify>
    Run `npm run dev`, log in as an admin, and walk through the following flows. For each step, confirm the described behavior matches — if any step fails, describe the failure and we'll triage; do NOT mark as approved with partial failures.

    1. **Sidebar navigation:** The "Departments" entry in the sidebar is a clickable link (no "Coming soon" aside). Click it → URL becomes `/departments`.

    2. **/departments page (DEPT-01 + INTR-01 + chart dimming):**
       a. Page renders with a rankings table, a metric toggle (Revenue | Sell-through | Lots above estimate), and a date range filter (L12M default).
       b. Switching the metric toggle re-sorts the table to the new metric DESC.
       c. The chip bar above the line chart shows the top-5 departments selected by default.
       d. The line chart renders one line per selected chip (different colors from CHART_PALETTE).
       e. The stacked bar chart below shows top-8 segments + "Other" gray segment; the stack visually sums to 100% on every bar.
       f. Click a row in the rankings table → matching row gets a soft blue tint + left border; non-matching lines in the line chart dim; non-matching segments in the stacked bar chart dim; a "Filtering: {CODE}" chip appears next to the date filter. Click the same row again OR the chip's × button → everything returns to full opacity.
       g. Try to select a 9th chip (after the default 5 + 4 more = 9) → a "Max 8 departments — deselect one first" status line appears briefly below the chip bar, then fades.

    3. **/sales selection flow (SALE-04):**
       a. Navigate to /sales. Each row has a leading checkbox.
       b. Check one row → a sticky footer appears with "Clear selection" on the left and a disabled "Compare (1)" button on the right + hint "Select at least 2 sales to compare".
       c. Check a 2nd row → "Compare (2)" becomes an accent-filled button.
       d. Check rows 3 and 4. Then click a 5th row's checkbox → the row does NOT become selected; "Max 4 sales — clear one to add a different sale" appears above the footer briefly then fades.
       e. Click "Compare (4)" → navigates to `/sales/compare?sales=...` with 4 comma-separated sale numbers.

    4. **/sales/compare (SALE-04 + SALE-05):**
       a. Page shows "Compare Sales" heading + "Comparing 4 sales" subtitle.
       b. Table renders metrics as rows, sales as columns; first column is sticky left with the metric labels.
       c. Metric-group heading rows appear: "Sale metadata", "Lot metrics", "Financial breakdown", "Participation".
       d. Every numeric cell in columns 2-4 shows a delta below the value (e.g. `+12.4%`, `-3.9%`, `0.0%`); first column has no delta; metadata rows (Sale date / Title / Payment status) have no delta.
       e. Deltas are color-coded: green (emerald) up, red (rose) down, gray flat.
       f. Sell-through delta uses `pp` suffix (e.g. `+3.2pp`), not `%`.
       g. "← Back to sales" link returns to /sales; your selection should still be highlighted (optional — behavior is browser-state dependent).

    5. **Invalid URL handling:**
       a. Manually navigate to `/sales/compare?sales=only-one`. Page renders "Invalid comparison" heading + "Back to sales" CTA.
       b. Navigate to `/sales/compare` (no params). Same invalid card.
       c. Navigate to `/sales/compare?sales=<script>` (malformed). Same invalid card.

    6. **Revenue Waterfall on Sale Detail (SALE-06):**
       a. Open any sale detail page. Below the Department Table, a "Revenue breakdown" ChartCard appears with a chevron toggle and a muted hint "Tap to see the path from hammer to net".
       b. Click the chevron → chart expands (≈320px tall). Chart shows 7 bars in order: Hammer (blue), +Premium (green), -Commission (red), -Insurance (red), -Lot charges (red), -Referral (red), Net revenue (blue).
       c. Hover a bar → tooltip shows the full step name + signed delta amount + (for intermediate steps) running total.
       d. Click the chevron again → chart collapses; chevron rotates back.
       e. Navigate to another sale → Revenue Breakdown is collapsed again (state is per-sale).

    7. **Cross-regression sanity:**
       a. Trends page still loads (no regression from chart color reassignments).
       b. Sales page without any selection still works exactly as before (no checkbox UI breaks sort/filter/navigation).
       c. Login and protected routing still gate all pages; unauthenticated access redirects to login.

    Return "approved" if all 7 flows behave correctly. Return a numbered list of failures otherwise.
  </how-to-verify>
  <action>
    Checkpoint — human-verify required. This verifies that automated tests did not miss any integration-level UI/UX issue: sticky-footer layout interplay, cross-filter visual smoothness, tooltip rendering under real browser layout, chart color accuracy, and the Revenue Breakdown collapse/expand transition — all of which are explicitly listed as manual-only in 06-VALIDATION.md.

    Operator steps:
    1. Run `npm run dev` in the project root.
    2. Walk the 7 flows above.
    3. If all pass, return "approved".
    4. If any fail, list failures by section number + what was expected vs observed.
  </action>
  <verify>
    <automated>echo "manual checkpoint — no automated verify"</automated>
  </verify>
  <done>Operator typed "approved" after walking all 7 flows.</done>
  <resume-signal>Type "approved" after completing the 7-flow walkthrough, OR list specific failures by section number.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NavLink href → router | Static string literal `/departments`; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Information Disclosure | Departments nav activation exposing unauthorized page | mitigate | `/departments` is registered inside `<ProtectedRoute>` (plan 06-02). The NavLink in DashboardLayout is itself rendered inside ProtectedRoute → unauthenticated users never see the sidebar. No new authorization surface introduced |
| T-06-06-02 | Tampering | static NavLink href | accept | Literal string in JSX; no concat, no user input |
| T-06-06-03 | Accessibility | sidebar NavLink keyboard + active-state indication | mitigate | React Router v7 NavLink is a standard `<a>` tag — keyboard-accessible by default. Active-state indicator (left-border accent) is a visual cue backed by `aria-current="page"` that NavLink sets automatically — AT support inherited |
</threat_model>

<verification>
- `npx vitest run src/tests/dashboard-layout.test.tsx` — passes
- `grep -c 'to="/departments"' src/layouts/DashboardLayout.tsx` returns ≥ 1
- `grep -c 'aria-disabled="true"' src/layouts/DashboardLayout.tsx` decreases by 1 compared to pre-plan state (one less disabled span)
- `grep -o "Coming soon" src/layouts/DashboardLayout.tsx | wc -l` returns 3 (Team, Reports, Custom Charts — NOT Departments)
- `npx tsc --noEmit` passes
- `npx vitest run` — full suite green
- Human checkpoint approved (Task 2)
</verification>

<success_criteria>
- Departments sidebar entry is an active NavLink.
- Team, Reports, Custom Charts remain disabled with Coming soon asides.
- Regression tests for DashboardLayout all pass.
- Human smoke check confirms all 7 end-to-end flows work (INTR-01 visual behavior, sticky footer layout, tooltip rendering, waterfall colors — all the items flagged as manual-only in 06-VALIDATION.md).
- Phase 6 is user-ready end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/06-department-analysis-sale-comparison/06-06-SUMMARY.md` capturing:
- Confirmation of the Departments NavLink flip + regression intact for other placeholders.
- Human smoke check results (which flows passed, any issues and their resolution).
- Phase 6 complete: map every REQ-ID (DEPT-01/02/03, SALE-04/05/06, INTR-01) to the plan + artifact that addresses it.
</output>
