---
phase: 01-schema-fixups
plan: 01
type: execute
milestone: crm-v05-demo
feature: crm-v05-demo
---

<objective>
Land the audit-log table (`private.api_usage`) needed by the CRM v0.5
classifier before the poller runs. The two CRM core tables
(`crm_threads` + `crm_classifications` + `crm_triage_queue` view) are
already in prod (migration 20260520120000 + fix 20260520130000 applied
2026-05-20). This phase covers the remaining additive migration
+ TypeScript types regen.

Deliverables:

- `tpc-dashboard/supabase/migrations/20260520140000_create_private_api_usage.sql` —
  creates the `private` schema if not present, then
  `private.api_usage(user_id uuid null, ts timestamptz default now(), model text,
  tokens_in int, tokens_out int, cost_usd numeric(10,4), app_source text,
  request_id uuid, metadata jsonb)` per D-015 spec. RLS: admins read; no
  client writes; service-role inserts only.
- Migration applied to prod via `npm run db:push`.
- `src/db/database.types.ts` regenerated via `npm run db:types`.
- `_workspace/Schema/schema.md` updated to include the new table.
- `_workspace/Schema/migrations.md` entry.

Success criteria:

- Migration list shows `20260520140000` Applied.
- `private.api_usage` exists with correct columns + RLS policies.
- `database.types.ts` includes `private.api_usage` typings.
- No existing CRM-table behaviour disturbed (additive only per D-005).

Not in scope: classifier service writes to this table (that's
phase 02-external-services); UI for the table (skip — admin observability
via Supabase Studio only for demo).
</objective>

<execution_context>
Run via `$HOME/.claude/skills/gsd-execute-phase/` — single plan, no waves.
Phase exits clean after migration applied + types regen + schema.md update
+ smoke test green (`npm run build`).
</execution_context>

<context>
@../../../../../_workspace/Features/crm-v05-demo.md
@../../../../../_workspace/Decisions/D-042-crm-v05-demo-on-dashboard.md
@../../../../../_workspace/Decisions/D-015-tpc-ai-proxy-full-logging.md
@../../../../../_workspace/Schema/schema.md
@../../../../../_workspace/Architecture/Decisions.md
@../../../../supabase/migrations/20260520120000_create_crm_v05_tables.sql
@../../../../supabase/migrations/20260520130000_fix_crm_triage_view_security.sql
</context>
